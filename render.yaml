services:
  - type: web
    name: xrpwhales-bot
    env: python
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python xrp_whales_bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.13
